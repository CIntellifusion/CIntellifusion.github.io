[toc]



# 菜狗踩坑之路



## 配置环境和依赖

### Torch TensorFlow Paddle 

目前已经跑过torch和Paddle的代码。 采取非必要不使用tensorflow的策略。 

pytorch的向前兼容的，所以一般来说问题不出现在pytorch代码上。 

Paddle的优势是 语法类似torch， 并且有官方的中文文档。 

然后deepspeech是百度做到，paddle也是百度做的。 这就是paddle构筑生态的第一步大概。 



### Conda 和pip 

```shell
>pip install -r requirement.txt 
> conda create -n {name } python = version 
> conda create -n pytorch python = 3.7
> conda install xx
```



#### Conda 环境管理



#### 包管理



### Linux 和 windows 

#### unbuntu 和linux 

在虚拟机上跑程序需要解决的环节包括：

​			代码编辑工具、 环境依赖，磁盘空间限制 

Paddle 是建议在linux上运行的，然后通过命令行训练也是linux更加完善。 

只是现在时windows主系统，磁盘空间更加的大。 





## 如何阅读别人的代码

> 程序员总是讨厌读别人写的代码。 当然，很久之前的我们也可以算作别人。 

 

 假设别人的代码是规范的？

阅读的时候显然需要假设别人的代码是规范的，但是在修改的时候需要假设别人的代码时存在隐形的问题的。



### 一个文件夹里面几个代码脚本

首先： 阅读readme 是一个好习惯 ，其次 ，init脚本是初始化脚本

然后，我们可能不知道脚本之间的关系了。 很简单（虽然我看了第二篇论文才相同

看调用关系

比如给一个models文件夹

|-__ init __.py

|-base_model.py

|-networks.py

|-audio2expresion.py

那么初步可以断定： 最先读init脚本，最后读 audio2expresion 因为audio2expression是最具体的

然后  我们需要点开  base_model 和 network两个脚本 

发现： network只import 了torch的子模块，而base_model 引用了networks.py 所以

init->networks->base_model->audio2expression的阅读顺序

## 关于辅助函数

可能会有很多helper_function,这些函数有点没几行，有的很长。

没几行的可能是包装函数，减少调用难度（虽然大大增加了阅读难度）

很长的可能有很多If-else 这样增强了包装的兼容性。 

## 关于神经网络类

有的作者可能会实现很多类，目的就是为了拆分。 实际上做的事无非两个：

1. 包装好类，让最终的代码好看 
2. 增加if-else 让 更改模型更容易，增加模型的选项。 

## 关于阅读顺序

我之前是按照文件夹读的

后来发现，还是从train.py函数开始读比较好

train.py -> dataset() -> dataloader()

​		 	-> model() -> forward()

​								-> backward()



## 关于代码风格

不要过度封装，并且写好注释，真的是对读代码的人的莫大的仁慈。

我也在想，这代码写的挺整齐的，就是太复杂了，究竟是谁的问题。 



什么样的神经网络代码是简洁美的 ：

train.py

dataset.py

model.py

test.py

driver.py

> 我看过一个开源项目，觉得作者的写法画蛇添足，然后我自己去写，一开始是简洁的代码。 一旦我开始弱化假设，代码逻辑就开始向着开源代码靠近了。 这件事的教训是：有时候觉得代码读不懂或者冗杂，可能只是假设条件有偏差。 





## 修改别人的代码和版本管理

```
python train/disentanglement/code/train_content+cla.py --max_epochs 200 --start_epoch 100 --pretrained_epoch 100
```

版本管理，尤其是在有很多文件处理的时候进行版本管理的时候是一个比较恼火的事情。 

git提供这样的版本管理工具。 

## 如何深入一个复杂的类

草，真蚌埠住了，这作者熟练掌握继承的手法。 

> 下面的都是我复制粘贴的，所以很简单，看完了不能用，但是能看代码了

## 辅助工具包



torchvision.transforms是**pytorch中的图像预处理包**，包含了很多种对图像数据进行变换的函数，这些都是在我们进行图像数据读入步骤中必不可少的。



torch.matmul是tensor的乘法，输入可以是高维的。
当输入都是二维时，就是普通的[矩阵乘法](https://so.csdn.net/so/search?q=矩阵乘法&spm=1001.2101.3001.7020)，和tensor.mm函数用法相同



#### attention tensor -> numpy 记得转cpu 在detach 和clone 





**内存映像文件**是一种将磁盘上的非常大的**二进制数据文件当做内存中的数组进行处理的方式**。NumPy实现了一个类似于ndarray的memmap对象，它允许将大文件分成小段进行读写，而不是一次性将整个数组读入内存。**memmap也拥有跟普通数组一样的方法，因此，基本上只要是能用于ndarray的算法就也能用于memmap。**

使用函数np.memmap并传入一个文件路径、数据类型、形状以及文件模式，即可创建一个新的memmap：

为存储在磁盘上的二进制文件中的数组创建内存映射。

内存映射文件用于访问磁盘上的大段文件，而无需将整个文件读入内存。NumPy的memmap是类似数组的对象。这与Python的mmap模块不同，后者使用类似文件的对象。

这个ndarray的子类与某些操作有一些不愉快的交互，因为它不太适合作为子类。使用此子类的另一种方法是自己创建mmap 对象，然后直接使用ndarray .__ new__创建一个ndarray，传递在其'buffer ='参数中创建的对象。

此类可能在某些时候被转换为工厂函数，该函数将视图返回到mmap缓冲区。



#### importlib



```python
import importlib
 
# 将aaa.py文件实例化为func对象
func = importlib.import_module('aaa')
 
# 执行aaa.py文件内的f1方法
func.f1()
 
# 获取mypackage.xxx文件的age属性
m = importlib.import_module('mypackage.xxx')
print(m.age)



```



### functools 模块

官方文档

https://docs.python.org/zh-cn/3/library/functools.html



## Image

bicubic插值算法，也被称为双三次插值算法，从纯数学的数值分析的角度，属于三次插值（可以理解为导数平滑的插值算法），类比于bilinear插值，值域平滑的插值算法，其保边能力更强；从信号与系统的角度，属于sa信号重建函数，在频域的理想低通滤波器。

##  hasattr()

**Python hasattr() function** is an inbuilt utility function, which is used to check if an object has the given named attribute and return true if present, else false.

## torch

### torch.nn.module.register_buffer

register_buffer的作用是将torch.ones(2,3)这个tensor注册到模型的 buffers() 属性中，并命名为a,这代表a对应的是一个持久态，不会有梯度传播给它，但是能被模型的state_dict记录下来。可以理解为模型的常数。

注意，没有保存到模型的 buffers() 或 parameters() 属性中的参数是不会被记录到state_dict中的，在 buffers() 中的参数默认不会有梯度，parameters() 中的则相反。

我们可以将前者理解为常数，后者理解为变量



### torch.tensor.expand_as()

2.expand_as（）函数：

     （1）函数功能：
    
              expand_as（）函数与expand（）函数类似，功能都是用来扩展张量中某维数据的尺寸，区别是它括号内的输入参数是另一个张量，作用是将输入tensor的维度扩展为与指定tensor相同的size。


类似于np.zero_like





### reflectionPad2d()

1）调用方式
输入形状为(N,C,H_in,W_IN)或(C,H_in,W_IN)；
输出形状为(N,C,H_out,W_out)或(C,H_out,W_out)；
padding表示填充尺寸，可为整数或者4元组；padding为整数时，输入向量左右上下填充相同的大小；而padding为元组时可自定义向量左右上下分别填充多少

### torch.backends.cudnn.benchmark=True 

设置 torch.backends.cudnn.benchmark=True 将会让程序在开始时花费一点额外时间，为整个网络的每个卷积层搜索最适合它的卷积实现算法，进而实现网络的加速。适用场景是网络结构固定（不是动态变化的），网络的输入形状（包括 batch size，图片大小，输入的通道）是不变的，其实也就是一般情况下都比较适用。反之，如果卷积层的设置一直变化，将会导致程序不停地做优化，反而会耗费更多的时间。





### **torchvision.transforms:** 常用的图片变换，例如裁剪、旋转等；



本文的主题是其中的torchvision.transforms.Compose()类。这个类的主要作用是串联多个图片变换的操作。



### torch.view



# 辅助语言 

为了让项目更快的运行，`专业的开发者` 会用一些脚本封装调用方式，直接按照作者给定的默认参数运行。 



## 各种shell: bash perl  cmd bat 

shell是一种脚本语言，通过对应的脚本解释器解释执行，一般作为内置于操作系统的应用程序向用户提供访问操作系统内核的服务。不过shell也分化出了很多种类，常见的有shell(/bin/sh)、bash(/bin/bash)、csh(/usr/bin/csh)、ksh(/usr/bin/ksh)、powershell(windows的shell)等，大部分shell语法互相兼容，其中bash是linux默认的shell

使用一些命令行解释器来批量调用python脚本

同时还可能配合yaml文件等等。 

用了这些东西，似乎显得我们成为了一个合格的开发者，或者科研人，

#### python 本身也是脚本语言 

我们可以把默认参数放在一个driver.py文件里，然后用python向系统发送命令行指令，运行代码

```python
# an example for driver.py
import os
cmd = 'python  train.py '
cmd +=' --gpu True '
cmd += ‘  --epoch 100  ’
cmd += ‘ --pre_train False ’
cmd+= '  '#other options that may require
print('using comman' , cmd)
os.system(cmd)

```



# JSON YAML XML 



JSON是一种文本文件，是JS语法的子集，面向对象，容易阅读，容易解析，可以用来构造数据集。 

